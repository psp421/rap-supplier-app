{
  "name": "rap-supplier-app",
  "version": "1.0.0",
  "description": "Fiori Elements App for BP Supplier Management (ABAP Cloud RAP)",
  "scripts": {
    "start": "ui5 serve",
    "build": "ui5 build --config=ui5.yaml --clean-dest --dest dist",
    "lint": "eslint webapp"
  },
  "devDependencies": {
    "@sap/ux-ui5-tooling": "^1.14.0",
    "@ui5/cli": "^3.0.0"
  }
}
